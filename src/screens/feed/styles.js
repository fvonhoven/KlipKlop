import { StyleSheet } from "react-native"

// Feed Screen styles
const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    backgroundColor: "#fff",
    padding: 10,
    borderRadius: 25,
  },
})

export default styles
